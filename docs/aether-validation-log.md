# Aether Validation Log

Tracks where Aether's abstractions fit, created friction, or required
extension during upgrade work. This is the generalization evidence.

## Format
**Date** | **Phase** | **Observation** | **Implication**

---

| 2026-05-23 | Local model infra | GPU acceleration (AMD RX 6600 XT / gfx1032) abandoned on Windows. Ollama community fork (ollama-for-amd v0.30.0) + gfx1031-gfx1032 rocblas libs installed correctly, but Ollama GPU discovery returns initial_count=0, total_vram=0B — only CPU detected. Known Windows-RDNA2 ROCm limitation, not a config error. Root cause of session instability was actually a near-full disk (532MB free), now cleared to 37GB. | Run local models on CPU (~15 tok/s, acceptable for dev/eval). GPU path deferred to a future WSL2/Linux setup as a separate project. Do not retry Windows ROCm. |

| 2025-05-23 | Cleanup/Baseline | flag_10pct_deviation e2e case fails: one-shot planner generates a plan that calls flag_item on all 6 rows instead of filtering to >10pp deviation first via run_sql. Critic correctly returns "fail" (loop working as designed). Root cause: planner reasoning failure, not a tool bug. | Fix deferred to RAO loop — per-step observation will let the model see intermediate SQL results before deciding what to flag. Do not patch the one-shot planner. |

| 2026-05-24 | Local planner test | phi4-mini (planner, Ollama, CPU) produced a structurally valid ExecutionPlan on first attempt but DROPPED the flag_item step — goal said "flag them if so", model planned load_data → run_sql → write_report, omitting flagging. Plan passed Pydantic (well-formed) but is semantically incomplete (misses the goal). 81s wall-clock, ~6 tok/s output, 1002 in / 484 out tokens. | First small-model decision-quality data point: 3.8B failure mode is SEMANTIC OMISSION, not malformed output. Comparison axis for the study: Anthropic baseline expected to include flag_item. Candidate fixes to test later: (a) stronger local planner (Mistral 7B), (b) prompt scaffolding emphasizing tool completeness. Do not fix yet — collect baseline first. |

| 2026-05-24 | RAO loop first run (phi4-mini) | Loop mechanics SOUND: failed tools became observations, loop recovered without crashing (granular recovery the one-shot run() cannot do), trace logged all 10 steps, mid-loop retrieve_context succeeded. Two small-model failure modes surfaced: (1) REASONING-ACTION DECOUPLING — model reasoned "retrieve_context should be used" but emitted tool="" eight consecutive steps; stated reasoning correct, structured output did not match its own reasoning. (2) Missing schema context — loop agent prompt lacked the file list + column schema the one-shot planner gets via _build_schema_block, so model picked wrong file (fund_agreement.txt vs fund_capital_accounts.csv) and omitted required table_name arg. Ran to max_steps, flag_item never reached, verdict fail. 166.6s, 10 steps. | Architecture validated; model is the constraint. Fixes: (a) empty-tool guard as a fed-back observation (not silent retry) so model can self-correct, (b) add schema/file block to loop agent prompt (port _build_schema_block). These are prompt/guard fixes, NOT architectural. After fixes, re-run to see if a schema-informed loop reaches flag_item — that remains the open architecture question. |

| 2026-05-24 | RAO loop (fixes applied, phi4-mini) | HEADLINE FINDING: observation-driven loop recovered the flag_item step that the one-shot planner DROPPED on the identical goal+model. Step 3 called flag_item on "Apex Ventures LP" AFTER observing the SQL result showing a partner >20% — the observation made flagging the obvious next action. Same model, same goal; difference is purely architecture (one-shot blind plan vs per-step observation). Loop self-terminated via is_final (not max_steps). Empty-tool guard fired 4x, recovered all. Mid-loop retrieve_context worked. | ARCHITECTURE VALIDATED — the agentic loop fixes a reasoning omission that blind planning could not, without a larger model. This is the core thesis demonstrated. |

| 2026-05-24 | RAO loop — residual model-quality issues | Three small-model failure modes remain, all decision-quality not architecture: (1) write_report skipped before is_final despite prompt requiring it (instruction-following gap); (2) redundant SQL at steps 2,5,6 — model re-ran the same filter query instead of progressing (steps-to-completion inefficiency); (3) critic (also phi4-mini) returned pass+critical-flag contradiction — Pydantic validator caught it and forced retry to fail, but the fail reasoning hallucinated ("failed to flag" when flagging occurred at step 3). | phi4-mini is the constraint on BOTH planner and critic roles. Clean signal to test Mistral 7B on planner. Validation layer working (caught the invalid critic output at the boundary). 267s / 7 steps with retries. |

| 2026-05-24 | RAO loop — planner model swap (Mistral 7B vs phi4-mini, identical goal) | SECOND HEADLINE FINDING: a LARGER model (Mistral 7B) completed the task FASTER end-to-end than the smaller phi4-mini (184s vs 267s) despite slower per-token CPU inference — because reliability beats raw speed: 3 steps vs 7, 0 empty-tool retries vs 4, ran the minimum viable path (load→query→flag→report→final) with no wasted steps. phi4-mini's size "advantage" was erased by its unreliability (wasted steps + retries). Full comparison: steps 7→3, retries 4→0, write_report no→yes, verdict fail→pass, wall-clock 267s→184s, both self-terminated via is_final. NOTE: critic was phi4-mini in BOTH runs — its messy verdict last run was partly because it judged phi4-mini's sloppy plan; given Mistral's clean plan, the same phi4-mini critic returned a correct pass first-attempt. Critic quality depends partly on plan quality. | For agentic decision quality on this workload, RELIABILITY (clean structured output, no wasted steps) matters more than model size or per-token speed. Mistral 7B is the planner model. Critic can stay phi4-mini — a clean plan is easy to judge; do not upgrade the critic preemptively. |

| 2026-05-24 | CORRECTION + infra bug | The retriever's persistent Chroma store + additive BM25 never cleared between runs — 448 orphan chunks accumulated across the session, contaminating ALL retrieval. This RETROACTIVELY EXPLAINS the first RAO run's "model picked wrong file" (fund_agreement.txt instead of the CSV): the polluted store surfaced fund_agreement chunks as top hits regardless of input file, so the model was fed the wrong corpus — that was an INFRA failure mis-attributed to the model. Caveat: the phi4-mini-vs-Mistral comparison also ran on the polluted store; both models faced identical pollution so the RELATIVE result (reliability beats size) still holds, but absolute per-run behavior was affected. | Fix: reset_index() clears collection + BM25 state per case (keeps models warm); eval uses EphemeralClient (no disk persistence); autouse per-case reset in both harnesses. Isolation verified before/after. LESSON: write the isolation test before trusting eval numbers — same shape as the "silent pass / empty output" bug (results looked fine, measured the wrong thing). Add to bug_stories. Baseline test_e2e.py numbers taken while polluted are SUSPECT — re-run both harnesses clean. |

| 2026-05-24 | Trace inspector built + first failure-landscape read | Promoted the hand-built SQLite trace store to a first-class debugging tool (aether/trace/inspect.py, two modes: per-run step view + cross-suite failure grouping; fully local, no Langfuse — the hand-built trace is the differentiator). FIRST USE collapsed a 15-case failure mess into root causes: 12/15 = identical malformed-JSON from Mistral ("Expecting ',' delimiter" at consistent char offsets — structural, repeatable, not random); 2/15 = run_sql window-function-in-WHERE CTE rewrite not triggering. (write_report 15/15 and no-run-end 12/15 are artifacts of the manual kill, not real failures.) Raw inspection revealed THREE distinct defect shapes: (1) response truncated mid-token ("tru" instead of "true" — model or stream cut-off); (2) raw[:500] cap in for_validation_error truncating valid-but-long JSON (trace capture bug, not model bug); (3) model emitting Python-style "..." ellipsis inside a JSON array (hallucinated shorthand). | LESSON: trace-based debugging turned N symptoms into 2 root causes in ONE read — vs three prior blind suite re-runs that revealed nothing. This is the observability payoff and a strong interview story: "I don't re-run to debug; I read the trace." Fix path: tolerant JSON repair in the parse layer (likely clears the 12/15) + investigate CTE rewrite for the 2 SQL errors. |

| 2026-05-24 | First TRUSTWORTHY full agentic suite run (Mistral, CPU, all fixes in) | ~4/13 completed cases passed (~31%), 78 min wall (~5 min/case). Run captured + inspected via trace tool. KEY SHIFT: after fixing the measurement instruments (trace truncation, JSON repair, state leaks), the DOMINANT failure moved from "12/15 invalid JSON" (mostly artifacts) to run_sql Binder errors 8/15 — model generates SQL referencing nonexistent columns (transaction_date, total_quarterly_dist), misuses GROUP BY, botches window-fn placement. Invalid-JSON dropped 12→6 (6 remaining are genuine, not capture artifacts). is_final+tool fix didn't move pass rate because loops now die EARLIER at SQL generation, before reaching write_report. | The real model-quality bottleneck is now visible and isolated: SQL generation correctness, not loop architecture, not JSON, not instrumentation. Four layers of debugging each revealed truer signal (broken trace → JSON noise → SQL errors). Next lever (next session): SQL robustness — stronger schema injection at the run_sql decision point, and/or feed the Binder error back into the observation so the model self-corrects column names on the next step. allocation_mismatch timed out (Ollama httpx socket stall, 10 min) — investigate separately. |

| 2026-05-24 | HEADLINE COMPARISON (corrected for partial-counting) — API vs local, identical agentic loop | Reporting clean-pass AND pytest-pass for honesty. gpt-4.1-mini: 9/15 clean pass (60%), 2 hard fails, 3 partial (hit 10-step ceiling on multi-doc compliance cases), 4 min. Mistral 7B Ollama CPU: 2/15 clean pass equivalent (prior "4/13" counted partials), 78 min. NOTE: earlier "87%" counted partial-as-pass — corrected. Lead with clean-pass; partials are step-ceiling timeouts, not successes. Same loop/prompts/retriever/harness for both. | THESIS: large API gap remains (60% vs ~13% clean), concentrated in SQL/reasoning correctness, not architecture. Step ceiling (10) too low for 3 multi-document compliance cases — separate issue from model quality. |

| 2026-05-24 | CRITICAL FINDING — frontier model fabricates from null results | gpt-4.1-mini on flag_5pct_deviation: SQL returned 0 rows (NULL SUM in a cross join), and the model FABRICATED a finding — invented "Northstar Growth Fund" with a 6.5pp deviation from empty output, and missed the real violator (Ironwood, 24.1pp). The critic caught the miss and returned fail. This reproduces the documented industry failure mode (agent fabricating numbers to force a reconciliation) IN OUR OWN SYSTEM on a capable model. | STRONG finding + architecture validation: even a frontier model hallucinates results from empty queries; the verifier-in-the-loop (critic) is what caught it. Argues directly FOR the critic/verification layer. Candidate fix: tool layer should make empty-result observations explicit/unmissable so the model can't fabricate over them. |

| 2026-05-24 | CORRECTION via trace check — ownership_tolerance_check is critic false-negative, not planner regression | gpt-5.4-mini "hard-failed" this case where gpt-4.1-mini passed. Trace shows BOTH planners did identical correct work: SQL returned 0 rows → fell back to initial context → correctly derived 92.0% total / 8.0pp deviation → flagged correctly. Difference was the CRITIC: gpt-5.4-mini's critic was stricter, filed critical flags for "no SQL evidence" and "no explicit calculation" while IGNORING the correct 92.0% calculation present in write_report output. verdict=fail, but the answer was right. | The fail is a critic false-negative (stricter critic penalizing absent SQL trail even when the answer is correct), NOT a planner regression. gpt-5.4-mini's effective quality is better than its raw hard-fail count suggests. |

| 2026-05-24 | UNDERLYING BUG surfaced — run_sql returns 0 rows on valid loaded tables | Across ALL three model runs, run_sql frequently returned 0 rows even though load_data succeeded and the table was registered. Models that "passed" SQL cases largely did so by BYPASSING the broken query — falling back to reading the CSV from initial retrieval context. This was masked until gpt-5.4-mini's stricter critic started penalizing the missing SQL evidence. | IMPORTANT CAVEAT on the whole SQL-failure narrative: some "SQL reasoning failures" are partly a run_sql TOOL bug (0 rows on valid tables), not pure model hallucination. Next-session priority: debug run_sql — table registered but query returns empty (likely table-name mismatch between load_data registration and the SQL the model writes). Fix this before drawing firmer model-quality conclusions on SQL cases. |

| 2026-05-24 | RETRACTION — "run_sql 0-rows bug" was an inspector parsing error, NOT a tool bug | The cols=[]/rows=0 readings came from the inspector calling .get('rows') on a result stored as a STRING (str(result)[:4000]), which always returns empty. run_sql works correctly — verified from the raw stored result strings (e.g. flag_5pct returned 2 violator rows). RETRACTS the earlier "run_sql returns 0 rows" entry AND the caveat that "some SQL failures are partly a tool bug" — that conclusion was based on the inspector's misread. ALSO RETRACTS the "CRITICAL FINDING — frontier model fabricates from null results" entry: gpt-4.1-mini did NOT fabricate from empty output; SQL returned 2 correct rows; model simply flagged only the first of two matching rows (incomplete flagging, not hallucination). Table-name mismatches DO occur occasionally but surface as loud DuckDB CatalogErrors, not silent empties. | Fix the INSPECTOR's result parsing (handle the stored string), not run_sql. Real failure causes are reasoning/critic issues, below. |

| 2026-05-24 | REAL FINDING (UPDATED) — "incomplete flagging" was observation-truncation, not a reasoning gap | flag_5pct_deviation: SQL correctly returned BOTH violators (Northstar 6.54pp, Ironwood 24.09pp). CONFIRMED: _summarise_observation capped at 200 chars. Ironwood's name started at char 187; its deviation value (24.09pp) was at char ~220 — beyond the cap. Model received "['Ironwood Vent..." with no values. gpt-4.1-mini saw Northstar first (full row) → flagged it. gpt-5.4-mini got a different row order so Ironwood appeared first → flagged it. Both models flagged correctly on what they could SEE; neither could see row 2's values. Pass/fail was decided purely by SQL row order, not model reasoning quality. IMPLICATION: ALL prior multi-row eval results across all three model runs are understated — the observation cap hid data on every multi-row case. Re-run required for trustworthy numbers. FIXED: _summarise_observation now formats whole rows up to 50 rows / 4000 chars, never truncates mid-row. Also fixed: args_short cap 120→400, critic executor_output cap 2000→8000. |

| 2026-05-24 | Two infra gotchas found during smoke test | (1) .env had MAX_RETRIES=3 overriding the code default — pydantic-settings reads .env over code, so the earlier "3→2" config change was silently ignored; ALL session runs (incl. phi4-mini vs Mistral comparison) actually used 3 retries regardless of config. Not a correctness issue (3 is safe) but a config-vs-reality mismatch. Removed the .env override. (2) Mistral prefixes its JSON action with prose ("Let me analyze..."); old _parse_action only stripped markdown fences, not leading text. New _extract_json_object walks first { to matching } — discards preamble. Qualifies the earlier "clean output" finding: Mistral emits CORRECT JSON WRAPPED IN PROSE; robust parsing required. | Lessons: (a) check .env when a config change seems ignored; (b) real models wrap structured output in chatter — parse defensively, don't assume bare JSON. Both add to the robustness story. Smoke test (ownership_over_20pct) passes clean through agentic harness in 277s. Clear to run full suite. |

| 2026-05-24 | GENERALIZATION PROVEN — engine reasons over a real non-finance document | Dropped in a real NVIDIA Rewards Program legal terms PDF (5 pages, text-only, no tables — a domain the engine was never built for). Ingestion: 23 clean chunks via pdfplumber. Agent retrieved the correct termination section (pages 3-4) and produced 6 correct, grounded termination conditions — verified against the document, no fabrication. ZERO finance contamination: model read tool descriptions, correctly never attempted load_data/run_sql on the PDF. Critic pass 0.97. | THESIS NOW TRUE: the engine is domain-general, not finance-shaped — same loop, same code, real legal document, correct grounded answer. The retrieval+synthesis core generalizes. This is the project's central claim, demonstrated. |

| 2026-05-24 | ARCHITECTURE FINDING — model short-circuits the synthesis tool | answer_from_context (the new auditable synthesis tool) NEVER FIRED. The model synthesized the answer inside its own loop-agent reasoning step (read chunks from the retrieve_context observation, extracted conditions, wrote them straight to write_report) — 2 steps instead of 3. Answer was correct, but synthesis was NOT a distinct auditable event: no grounded flag, no insufficient_context guard, no synthesis trace event, no separate token count. Critic passed on output correctness without verifying groundedness. | PRINCIPLE: a capability offered as an optional tool gets bypassed if the model can do it implicitly (shorter path wins). To enforce AUDITABLE synthesis, the loop must REQUIRE answer_from_context for text-reasoning goals — offering it isn't enough. Decision pending: force the tool (auditability, consistent with the project's thesis) vs allow implicit synthesis (simpler). Leaning force, given auditability is the differentiator and models demonstrably fabricate (see earlier null-result finding). |

| 2026-05-24 | GENERALIZATION CONFIRMED ACROSS 3 DOMAINS | Third test: real medical research paper (Frontiers in Neurology, post-stroke dysphagia, 10 pages, dense academic prose + tables, 72 chunks). Same engine, same code, now-domain-neutral prompts. Goal: "main finding + what did NOT improve." Agent retrieved the conclusion section and produced a CORRECT, grounded two-part answer (improved: swallowing/QoL/self-efficacy/PA; not improved: body weight/TSF/TP) — verified against paper, no fabrication. Now finance + legal + medical, three maximally distinct domains, all working on one engine. THESIS PROVEN three ways, not once. | The engine is genuinely domain-general. The retrieval+synthesis core handles tabular finance, legal prose, and academic medical text with no code changes. |

| 2026-05-24 | De-finance validated + remaining contamination isolated to fewshots | The de-financed critic correctly evaluated a MEDICAL answer: returned partial (not pass) with incomplete_coverage + missing_data flags because the answer omitted ALB, which the paper DOES list as non-improving — a real, verified gap the critic caught using the new domain-neutral categories (no finance categories fired). De-finance WORKS. Remaining contamination: the model attempted load_data on the PDF at step 0 (failed loudly, recovered in 1 step, no retry burned) — vs ZERO contamination on the NVIDIA legal doc. Diagnosis: hardcoded prompts are now neutral, but the finance FEWSHOTS (example plan starts with load_data) still anchor a load-first prior. | De-finance reduced but didn't eliminate the bias; the finance fewshots are the last finance-shaped layer. Next lever (future): a neutral or domain-appropriate fewshot set, OR fewshots that show retrieve-first for text goals. Low priority — the model self-corrects in one cheap step. |

| 2026-05-24 | STANDARD BENCHMARK — T2-RAGBench (FinQA single-turn sample) | 15-record FinQA single-turn sample, scored with proper Number-Match normalization (scale-tolerant /100, with a dual-condition guard so format fixes don't launder wrong answers). RESULT: 12/15 (80%) correct. Breakdown: 5 exact, 7 correct-arithmetic-wrong-format (÷100 % vs ratio — normalized to correct), 3 genuine misses: #05 wrong formula (interpretation), #10 wrong table row (model pulled the -530M total instead of the -35M line item), #06 benchmark annotation artifact (gold uses different source numbers than the question — not our error). CAVEATS: 15-record SAMPLE not full benchmark; FinQA single-turn only (ConvFinQA multi-turn excluded — no conversation state; harder TAT-DQA excluded). All 15 ran clean, 0 crashes, 0 max_steps. Contamination: 14/15 attempted load_data on the .txt context first (finance fewshot load-first prior), failed loudly, recovered in 1 step. | The engine does correct financial arithmetic on a standard benchmark (80% on the fair slice). Misses concentrate in TABLE-ROW-TARGETING (#10) — traceable to flattened-table chunking scattering rows across windows; motivates a SQL-routing path (parse markdown table -> DataFrame -> load_data -> targeted run_sql). Number-format mismatch was the dominant raw-score depressor (would have falsely read ~33% without normalization) — another case of the instrument needing correction before the number is trustworthy. |

| 2026-05-24 | SQL-ROUTING for table questions — before/after on FinQA sample | Built markdown-table -> DataFrame parser, routed benchmark tables through load_data so the agent targets rows with run_sql instead of reasoning over flattened table text. SAME 15-record sample, clean before/after: text-only 12/15 (80%) -> SQL-routing 13/15 (87%). 1 gain, 0 regressions. Record #10 fixed exactly as predicted: agent queried the specific table row (balance dec 31: 1136 vs 1171) -> -35 correct, vs -530 total from flattened text before. Remaining 2 misses correctly unchanged: #05 formula/interpretation error (SQL provides right numbers, can't fix wrong usage), #06 benchmark annotation artifact (gold is wrong). | CAUSAL RESULT: table-row-targeting errors traced to flattened-table chunking; routing tables through DuckDB fixed the predicted failure with no regressions. SCOPE CAVEAT: all 15 T2-RAGBench tables share a uniform pandas.to_markdown() shape (machine-generated, regular) — parser handles these cleanly, but real-world irregular financial tables (merged cells, multi-row headers, footnotes) would exercise the fallback path more; robust real-table parsing is a future step. The fix is validated on the benchmark's regular tables, not claimed for arbitrary PDFs. |

| 2026-05-24 | FINANCE BENCHMARK at scale (n=200) — REAL number, corrects the inflated 15-sample 87% | RETRIEVAL (200 queries over 380 unique contexts / 2,476 chunks): Recall@1 0.68, Recall@3 0.805, Recall@5 0.86, MRR@3 0.738, nDCG@5 0.778. 68% rank-1; 28 complete misses (generic financial vocab matching wrong company's context). END-TO-END (200 FinQA records, gpt-5.4-mini, SQL-routing on, Number-Match scored): 137/200 (68.5%). Failure breakdown: 54 formula/interpretation/scale (41 of these ratio/return interpretation — model gets right numbers, wrong calculation), 13 large-scale-number format, 6 max_steps, 5 premature is_final, 1 API error. SQL path: 200/200 tables parsed, agent used SQL on 154/200 (77%); 45 SQL-using answers still wrong (SQL fixes row-targeting, NOT formula errors). | THE REAL NUMBER IS 68.5%, not 87%. The 87% was an easy-subsample artifact (first 15 records are simpler) — corrected by scaling to n=200. KEY DIAGNOSIS: 86% retrieval recall vs 68.5% end-to-end = an 18-point gap that is REASONING, not retrieval. The model finds the right context but constructs the wrong financial calculation on ~20% of cases. Bottleneck is multi-step financial-reasoning correctness, not information retrieval. CORRECTIONS: corpus is 380 contexts not 1,147 (contexts shared across questions); real token cost is ~13,600/record not 8,500 (15-sample underestimated because early records were simpler). Next levers: formula/interpretation errors (the dominant failure) — better fewshots showing correct calc conventions, or a verification step on the arithmetic; company-disambiguation in retrieval for the 28 cross-company misses. |

| 2026-05-24 | CAPSTONE — anti-fabrication guard works; answer_from_context fires correctly on sparse evidence | Asked the NVIDIA legal doc for a specific 1099 dollar threshold the document does NOT state (it references "the minimum amount required by US IRS regulations" without the figure — a trap: topic present, specific fact absent, the classic fabrication-from-null bait). Result: retriever found the correct clause; model recognized the number was absent; answer_from_context FIRED (first time across 3 tests) returning insufficient_context=True, grounded=False; verbatim answer refused to supply a figure and cited the exact page. NO fabrication, no "$600" from world knowledge. Critic: partial / missing_data (correct — unanswerable question correctly not answered; unsupported_claim correctly did NOT fire since no claim was invented). | RESOLVES the "is answer_from_context dead weight" question: NO. It's a dormant guard that fires exactly when evidence is sparse — the model ESCALATED to it on its own (step 2 implicit synthesis → step 3 explicit guard when it hit the unanswerable specific). The optional-tool design (not forced) let the model choose the guard when it mattered. The fabrication-from-null failure mode (observed earlier in gpt-4.1-mini) is now demonstrably guarded against. This is the engine's core trustworthiness property, proven on a designed trap. |

| 2026-05-24 | Chunk-size + overlap sweep (free, deterministic retrieval eval, n=200) | Swept chunk_size {512,800,1024,1500} and, at 512, overlap {50,100,128}. DECISION: chunk_size=512, overlap=100. | 512/100 beats 800/100 by ~+2pp R@3 (0.810 vs 0.790) on the replication run; R@5 gain (+0.5pp, 0.850 vs 0.845) is within noise; ~33% faster queries (463 vs 691ms, via smaller rerank payload). IMPORTANT VARIANCE NOTE: an earlier sweep run measured 512 R@3 at 0.845 (+5.5pp); this run got 0.810 (+2pp). The ~3.5pp swing on the SAME config means the retrieval variance envelope is ±2-3pp, WIDER than the ~1.5pp first assumed — so 512's true R@3 gain is modest (~+2pp), not the +5.5pp the first run suggested. Overlap 50≈100 (identical, extra chunks bought nothing); 128 WORSE (R@3 0.775, redundant near-duplicate chunks dilute ranking). | The switch is justified by the modest-but-directional R@3 gain + the robust latency win, NOT by a large recall jump. Headline retrieval numbers carry a ±2-3pp error bar; report as such, not as false-precision single values. |

| 2026-05-24 | Number-Match normalizer extended — corrected n=200 score, integrity-checked | Extended the scorer to recover convention/format mismatches the original guard missed: /100 for gold>1.0 (313.11% = 3.131), boolean True/False = yes/no, and conservative power-of-1000 unit scaling (with abs-magnitude guards). Re-scored the SAME n=200 answers (no agent re-run): 137 -> 151/200 (68.5% -> 75.5%), +14. INTEGRITY VERIFIED: 0 false positives — all 23 B (wrong-row), all 10 C (reasoning), all 5 D (benchmark-artifact) records STAYED failed. Tight guards specifically blocked finqa_test_22 (93.21/1000≈gold) and finqa_test_53 (share price 7.47 vs gold 7.47e-6). 5 A-records correctly stay failed (sign errors, wrong formula — genuine errors, not format). | HONEST NUMBER: 75.5% (n=200, single run, corrected normalizer). The +7 over 68.5% is recovered CORRECT answers that were mis-scored, not inflation — verified by the B/C/D-stayed-failed check. Residual: ~23 wrong-row (partly fixable: SQL/year-alignment), ~10 reasoning-ceiling (model limit), ~5 benchmark artifacts (not engine error). PENDING: variance re-run tomorrow to report mean ± deviation and lock the headline number. |

| 2026-05-24 | Grounded visual output shipped end-to-end; PDF table extraction fixed | **DECISIONS:** (1) Replaced pdfplumber extract_tables() (returned 0 tables on text-layout financial PDFs) with Camelot stream-mode in load_data's PDF branch. Camelot extracted all 8 tables from the test income statement where pdfplumber found 0. Local, free, no API — preserves the local-first principle. (2) load_data now registers ALL extracted tables with a manifest (table name, page, columns) rather than only the largest; the agent picks the right table from the manifest. Largest-table alias still points income_stmt → intro text; latent, did not bite, fix when touched. (3) render_visual produces a grounded Vega-Lite spec from run_sql results only; values copied verbatim from tool output, never model-generated. **MEASUREMENTS (verified end-to-end, eyes-on):** "Show me a bar chart of revenue, gross profit, operating expenses for 2002" → load_data (Camelot, 8 tables) → run_sql income_stmt_2 → render_visual grounded:true → chart rendered via vl-convert PNG + Streamlit st.vega_lite_chart. Values exact: Revenue 1,104,786 / Gross Profit 364,672 / Operating Expenses 286,817, all traceable bar→spec→SQL row→PDF cell. Verdict pass. Earlier $3 gross-profit error (recompute-from-misread on the text path) eliminated: SQL path reads the exact line-item value. Grounding banner verified on both refusal channels (answer_from_context insufficient_context; critic fail+missing_data). | **CONSTRAINT BOUNDARY (for demo honesty):** Works cleanly on tables with plain numerics (income statement). Balance sheet, cash flow, and notes tables do NOT coerce to numeric yet — parenthesized negatives "(16,149)" and "--" dashes block pd.to_numeric, so those tables are queryable only as strings. Arithmetic/charts on those statements not yet supported. **NEXT (all optional polish, none blocking a demo of the core):** Parenthesized-negative + dash coercion for balance sheet/cash flow; fix income_stmt alias (points at intro text); bubble-style trace visualization (cosmetic); strategy fork still open: ComplianceOS vs FinanceBench research paper. |

| 2026-05-24 | Stress test: engine refuses to fabricate; Camelot table-split limit found | **STRESS TESTS RUN (adversarial, on the income-statement PDF):** (1) "Current assets vs current liabilities 2002" → FAIL (max_steps, run b279d204). Engine searched 6 distinct tables (acct_pdf_1 through _7) + a LIKE filter for current liabilities, found assets (216,608), never found liabilities, did not fabricate. 9 run_sql calls, no repeated query until step 9. Root cause diagnosed: Camelot stream mode split the page-2 balance sheet and registered only the ASSETS half (11 rows); the LIABILITIES section ($121,640: bank overdraft, accounts payable, income tax payable) was never extracted into any table. Engine behavior was correct given incomplete input — searched honestly and failed honestly rather than inventing numbers. (2) "Five-year profitability trend" (doc has only 2 years) → PARTIAL. Engine found 2002+2001, did NOT fabricate three missing years. Critic flagged incomplete_coverage correctly. This is the key fabrication-honeypot result: the engine refused to hallucinate data to satisfy a chart request. | **FINDINGS:** (1) Grounding discipline holds under stress: in both adversarial cases the engine declined to fabricate, which is the critical property for a finance/compliance tool. (2) Real limitation found — Camelot stream mode splits multi-section tables (balance sheet Assets/Liabilities on one page → only Assets captured). Silent failure: the partial table looks valid; the engine can't know half is missing. This is the concrete, diagnosed justification for a layout-aware parser (LlamaParse/Reducto tier) as future work. (3) Minor engine improvement: on genuinely-absent data the engine searches to max_steps instead of writing a partial report and stopping early. Graceful-degradation rule is a clean future fix. **CONSTRAINT BOUNDARY (demo honesty):** Reliable on single-section clean-numeric tables (income statement — charts verified exact). Fails HONESTLY (no fabrication, FAIL/PARTIAL verdict) on split tables (balance sheet) and non-coercing tables (cash flow). Demo on the income-statement path; present the balance-sheet case as the honest-refusal boundary. |

| 2026-05-24 | Financial-number coercion shipped; 700-row aggregation verified; constraint boundary sharpened | **DECISIONS:** Added _coerce_financial_value / _coerce_financial_column / _clean_dataframe to load_data.py. Handles real accounting notation: strips $/£/€/¥, whitespace, quotes; "(x)" → -x (parenthesized negatives); "$-"/dash → null; removes ALL commas (handles irregular grouping like "5,29,550"); pd.to_numeric(errors=coerce). Column converts to numeric only if ≥80% of non-empty values parse (protects categorical columns like Segment/Country). Applied to CSV, Excel, and PDF paths (replaces prior ad-hoc comma-strip). **MEASUREMENTS (verified):** (1) 700-row Financials.csv (Kaggle): all money columns coerced correctly ($-, commas, quotes handled); Segment/Country/Product correctly kept as strings. "Total profit by segment" → SQL GROUP BY/SUM over 700 rows → grounded bar chart. Engine SQL totals matched INDEPENDENT pandas groupby EXACTLY for all 5 segments (incl. Enterprise -614,545.62, a loss segment) and grand total 16,893,702.29. Self-corrected on attempt 2 after a needless string-replace SQL. (2) Income statement PDF: no regression, GROSS PROFIT still 364,672. (3) Cash flow PDF: parenthesized negatives now coerce — (16,149) → -16149.0, previously blocked as strings. (4) Balance-sheet re-test ("current assets vs current liabilities 2002"): still FAIL at max_steps. Confirmed NOT a coercion issue — numbers that exist parse correctly; the liabilities section ($121,640) is still absent from all 8 extracted tables because Camelot splits the page, not because values don't parse. | **CONSTRAINT BOUNDARY (current, sharpened):** FIXED this session: accounting-notation coercion ($, commas, parens, dash-zeros). Real financial CSVs/tables with messy formatting now parse and aggregate correctly. STILL OPEN (two distinct, diagnosed limitations, neither blocking income-statement / CSV demo paths): (1) Camelot stream mode SPLITS multi-section tables — balance sheet assets/liabilities extracted as assets-only; liabilities never captured. Fix = layout-aware extraction (LlamaParse/Reducto tier). (2) No graceful termination on unfindable data — engine loops to max_steps instead of concluding "data absent, reporting partial." Fix = stop-after-N-non-advancing-queries + partial report. **DEMO-READY PATHS (verified end-to-end this session):** Income statement PDF → exact single-value + comparative bar charts. 700-row Financials.csv → SQL aggregation (group/sum) → grounded chart, matched to independent ground truth. **NEXT (presentation, not build):** README/resume/website update (lead with honest numbers); demo recording on the two verified files; strategy fork (ComplianceOS vs FinanceBench research paper) still parked. |
